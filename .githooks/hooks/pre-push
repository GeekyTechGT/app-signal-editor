#!/usr/bin/env sh
set -eu

if git diff --quiet && git diff --cached --quiet; then
  exit 0
fi

echo "pre-push: working tree or index not clean" >&2
echo "pre-push: commit or stash pending changes before pushing" >&2
exit 1

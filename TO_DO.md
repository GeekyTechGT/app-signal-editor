Applica le modifiche necessarie per :
1. l'utente deve poter caricare piu curve all'interno dello stesso plot
2. solamente uno alla volta deve essere attivo
3. quando l'utente aggiunge o rimuove un sample alla curva attiva, la rimozione o aggiunta deve essere fatta anche su tutti gli altri segnali del file perche tutti i segnali devono avere la stessa dimensione in termini di samples. Per il segnale non attivo, devi aggiungere come valore, l'interpolazione tra il valore associato all'istante t-1 e il valore associato all'istante t+1, dove t0 è l'istante temporale in cui l'utente aggiunge il sample.
4. l'utente deve poter aggiungere un offset su tutti i samples del file (sia da plot: crea un opzione nel context menu quando clicchi tasto destro del mouse, sia su tabella), oppure deve poter aggiungere un offset solamente a un sample (sempre selezionabile con tasto destro del mouse).
5. l'utente nel plot deve poter trascinare anche la linea che collega due samples: quando trascinata devono trascinarsi anche i sample di estremita della linea e di conseguenza rimodellarli i collegamenti dei samples estremi con gli altri samples della curva.
6. nel file csv/xlsx l'asse del tempo ha nome 't' o 'time': se non lo trovi, non caricare il file; quindi non è detto che l'asse del tempo si trovi nella prima colonna del file. i valori dei segnali sono collocati dalla colonna successiva a quella del tempo; crea un file di test e verifica sia file corretto che file sbagliato.
7. i segnali con valori enumerativi devi gestirli diversamente: nel file non sarà piu presente il mapping stringa:valore: l'app quando carica il segnale, alla prima stringa che trova assegna valore 0, alla seconda stringa diversa dalla prima valore 1 e cosi via; l'utente poi potrà modificare il mapping nella finestra di opzioni del singolo segnale; quando viene modificato a applicato il nuovo mapping, devi fare un refresh della curva nel plot e nella tabella.
8. esiste la possibilità in qt di fare lo screenshot del contenuto del plot? se si, integra la funzionalità
-------
se  faccio uncheck del segnale attivo l'app non crash piu, ma se faccio di nuovo check del segnale, l'app va in crash. Inoltre se faccio uncheck del segnale attivo, il colore dell'item della lista dei segnali devi ripristinarlo e non lasciarlo evidenziato.